# CONTEXT.md — {{WORKSPACE_NAME}} (L1, reception)

**Identity + base rules are in `AGENTS.md` — read it first.** This file is the **map + routing** (where to
go) plus the overview and layer map.

## Routing

| Task | Go to |
|------|-------|
{{ROUTING_ROWS}}

## How the work flows

{{HOW_WORK_FLOWS}}

## Layers

- L0 root `AGENTS.md` — what this place is + how to enter + base rules
- L1 this `CONTEXT.md` — overview
- L2 each dept/room/sub-room `CONTEXT.md` — the contract
- L3 each `docs/` — references + memory (read-only during a run)
- L4 the product layer — `projects/<run>/...` (sequential) or a room-local `workbench-<id>/` (parallel)

Conventions: `docs/CONVENTIONS.md`.

## Map

```
{{FOLDER_TREE}}
```
